# Optional: used by docker-compose for build args and overrides.
# Copy to .env and fill; also ensure backend/.env and frontend/.env.local exist.
NEXT_PUBLIC_API_URL=http://localhost:8000
FRONTEND_URL=http://localhost:3000
